<!-- views/index.ejs -->
<!doctype html>
<html lang="en">
	<head>
		<title><%= settings.appTitle %> - Signup and Login</title>
		<% include partials/meta %>
	</head>
	<body role="document" class="intro">
		<div id='wrapper'>
			<div id='page-content-wrapper'>
				<div id="main" class="container" role="main">
					<div class="logo" style="background: url('<%= settings.logo %>') no-repeat top;"><span></span></div>
					<div class="slogan"><%= settings.appSlogan %></div>
					<div class="description"><%= settings.appDescription %></div>
					<!-- SIGNUP AND LOGIN -->
					<div class="jumbotron text-center">
					<% if (new Date().getTime() < new Date(settings.icoStart).getTime()) { %>
						<div class="description">ICO BEGINS <%= settings.icoStart %></div>
					<% } else { %>
						<% if (new Date().getTime() < new Date(settings.icoStop).getTime()) { %>
							<div class="description">ICO IS IN PROGRESS</div>
						<% } else { %>
							<div class="description">ICO IS CLOSED</div>
						<% } %>
					<% } %>
						<div class="countdown-dates" id="icostart"><%= settings.icoStart %></div>
						<div class="countdown-dates" id="icostop"><%= settings.icoStop %></div>
						<div class="countdown" id="countdown"></div>
					<!-- show any status messages from server -->
					<% if (settings.status.length > 0) { %>
						<div class="alert alert-danger"><%= settings.status %></div>
						<p>We appologize for the inconvenience, and are working to resolve the issue.</p>
						<p><br><a href="<%= settings.chRoot %>/" class="btn btn-primary btn-lg"><span class="fa fa-home"></span> Try Later...</a></p>
					<% } %>
					<% if (settings.status.length == 0) { %>
						<a href="signup" class="btn btn-primary btn-lg"><span class="fa fa-user"></span> Signup</a> &nbsp;
						<a href="login" class="btn btn-primary btn-lg"><span class="fa fa-sign-in"></span> Login</a>
					<% } %>
					</div>
				</div>
			</div>
			<div class="footer text-with-background text-centered">
				<% include partials/footer %>
			</div>
		</div>
	</body>
</html>
